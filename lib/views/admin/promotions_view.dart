import 'package:flutter/material.dart';

class PromotionsView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Promotions')),
      body: Center(child: Text('Interface de gestion des coupons à venir...')),
    );
  }
}
